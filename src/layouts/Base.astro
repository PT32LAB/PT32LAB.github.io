---
import { ClientRouter } from 'astro:transitions';
import '../styles/global.css';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
  dark?: boolean;
}

const { title, description = 'BARK — Bolivian Ark. An off-grid community preserving humanistic values through resilient technology.', dark = false } = Astro.props;
---

<!doctype html>
<html lang={Astro.currentLocale || 'en'}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <link rel="alternate" hreflang="en" href={`https://pt32lab.github.io/en${Astro.url.pathname.replace(/^\/(en|es)/, '')}`} />
    <link rel="alternate" hreflang="es" href={`https://pt32lab.github.io/es${Astro.url.pathname.replace(/^\/(en|es)/, '')}`} />
    <link rel="canonical" href={`https://pt32lab.github.io${Astro.url.pathname}`} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@400;500;600;700;800&family=Lora:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet" media="print" onload="this.media='all'" />
    <noscript><link href="https://fonts.googleapis.com/css2?family=Libre+Franklin:wght@400;500;600;700;800&family=Lora:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet" /></noscript>
    <title>{title} | BARK</title>
    <meta property="og:title" content={`${title} | BARK — Bolivian Ark`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="https://pt32lab.github.io/images/yungas/valley.jpg" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="BARK — Bolivian Ark" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={`${title} | BARK`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="https://pt32lab.github.io/images/yungas/valley.jpg" />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "BARK — Bolivian Ark",
      "url": "https://pt32lab.github.io",
      "description": description,
      "foundingDate": "2026",
      "foundingLocation": {
        "@type": "Place",
        "name": "Coroico, Yungas, Bolivia",
        "geo": { "@type": "GeoCoordinates", "latitude": -16.18, "longitude": -67.72 }
      },
      "sameAs": ["https://github.com/PT32LAB"]
    })} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "BARK — Bolivian Ark",
      "url": "https://pt32lab.github.io"
    })} />
    <ClientRouter />
  </head>
  <body class="min-h-screen flex flex-col">
    <Nav />
    <main class="flex-1" transition:animate="fade">
      <slot />
    </main>
    <Footer />

    <script>
      import { init } from '../scripts/init';
      document.addEventListener('astro:page-load', init);
    </script>
  </body>
</html>
